// Top-level build file where you can add configuration options common to all sub-projects/modules.
plugins {
    // Apply the Android Application plugin to this project.
    // Make sure version is compatible with your chosen Gradle version and AGP features you use.
    id 'com.android.application' version '8.2.1' apply false // Example AGP version
    // id 'org.jetbrains.kotlin.android' version '1.9.22' apply false // If using Kotlin
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
